/**
 * ====================================================================
 * @wangly 基础类型校验
 * ====================================================================
 */

export const isNumber = (val: number): boolean => {
  return typeof val === 'number'
}
